function countVowels (str,count) {
    if (str.includes("a", "e", "i", "o", "u"));
        for (let i = 0; i < array.length; i++) {
    if (array[i] === target) {
      count++;
    }
  }
  return count;
}

module.exports = {
    countVowels
}